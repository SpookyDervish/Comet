struct Box <T> {
    T value

    init(T value) {
        self.value = value
    }
}


func main() -> int {
    breakpoint
    Box<int> box = new Box<int>(123)
    Box<bool> box2 = new Box<bool>(1)
    return 0
}
import testlib

struct Test {
    int x

    init(int x) {
        self.x = x
    }

    func foo(int a, int b) -> int {
        return a + b
    }
}

func main() -> int {
    testlib.Foo testStruct = new testlib.Foo(10)
    return testStruct.add(3, 4)
}
struct Foo {
    int x

    init(int x) {
        breakpoint
        self.x = x
    }

    func test() -> int {
        return self.x
    }
}

func main() -> int {
    Foo foo = new Foo(123)
    foo.x = 50

    return foo.test()
}
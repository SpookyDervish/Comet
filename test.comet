struct Bar {
    int y

    init(int y) {
        self.y = y
    }
}

struct Foo {
    Bar x

    init(Bar x) {
        self.x = x
        x.y = 50
    }
}

func main() -> int {
    Foo x = new Foo(new Bar(123))
    return x.x.y
}
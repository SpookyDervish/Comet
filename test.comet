struct Child {
    mut int x

    init(int x) {
        self.x = x
    }
}

struct Parent {
    mut Child c

    init(Child c) {
        self.c = c
    }
}

func main() -> int {
    Parent parent = new Parent(new Child(123))
    parent.c.x = 124
    return parent.c.x
}

struct Box <T> {
    T value

    init(T value) {
        self.value = value
    }

    func add(T amount) -> void {
        self.value += amount
    }
}

func main() -> int {
    Box<int> myBox = new Box<int>(1)
    myBox.add(2)

    return myBox.value
}
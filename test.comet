import io

struct Foo {
    int value

    init(int value) {
        self.value = value
    }

    as string {
        return io as string
    }
}

func main() -> int {
    Foo test = new Foo(10)
    io.println(test as string)

    return 0
}
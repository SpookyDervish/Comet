import src.token

struct Lexer {
    string sourceCode

    int pos = 0
    int column = 1
    int row = 1
    small currentChar

    init(string sourceCode) {
        self.sourceCode = sourceCode

        self.currentChar = self.sourceCode : self.pos
    }

    readonly func lex() -> void {
        while self.pos < #self.sourceCode {
            
        }
    }

    private func consume() -> void {
        self.pos += 1
        self.currentChar = self.sourceCode : self.pos

        if 
    }
}
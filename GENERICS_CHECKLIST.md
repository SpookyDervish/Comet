1. [X] Parse generic types
2. [X] Declare generic types when we see a struct def
3. [ ] Whenever we see a `new`, if its for a generic struct we create a new struct with the given types, and cache it if we need to